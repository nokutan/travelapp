<h1>Plans</h1>

<%= button_to "Create", new_plan_path, :method => :get %>


<% @plan.each do |plan| %>
    <ul>
        
                <div class="sen1">
                <div class="boxtitle"><%= link_to plan.title, plan_path(plan.id) %></div>
                </div>
    </ul>

<% end %>